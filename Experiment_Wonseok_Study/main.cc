
#include <iostream>
#include <cstdlib>
#include <string>
#include <time.h>

#include "abstracted_control_system.h"

int main(int argc, char *argv[])
{
	if (argc != 4)
	{
		std::cerr << "main(argc, argv)::";
		std::cerr << "invalid usage" << std::endl;
	}

	srand(0);
	
	
	
}